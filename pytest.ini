[pytest]
# Solo recolectamos desde tests/ (adentro se envuelven las suites históricas y
# viven las pruebas nativas nuevas como el E2E). Evita doble-recolección de los
# scripts test_*.py de la raíz.
testpaths = tests
python_files = test_*.py
addopts = -q
